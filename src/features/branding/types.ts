export interface WorkspaceBranding {
  businessName: string;
  logoInitial: string; // placeholder until real logo upload exists
  primaryColor: string; // hex, used for the booking widget's accent
}
